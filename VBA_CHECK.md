# VBA点検結果

点検日：2026-09-15

対象：`従業員マスター.xlsm`（`330bf0cd-_______.xlsm` も同一内容）

SHA-256：`5ee5c0b2c7d334434a3dbe097048e608c4eba6ef78ddc17a1e1ddc31d76594d9`

追記：シート4の読み込みエラーに対応し、XML要素の順序を修正した。VBA本体はコミット `f268604` と同一。公式スキーマによる修正前後の検証結果は [XML_CHECK.md](XML_CHECK.md) を参照。

## 修正した箇所

| 箇所 | 問題と対応 |
| --- | --- |
| ファイル形式 | 先行コミット `3aa7c36` のマクロ有効ブック用 Content Types 修正を引き継いだ。 |
| AppendRecords / SaveTargets | 失敗時に `On Error Resume Next` のまま行削除を繰り返すため、削除できないと無限ループする。回数が限定された削除処理に変更し、復元の成否を判定する。 |
| SaveTargets | 保存前の台帳が0行の場合、DataBodyRangeが存在せずエラーになる。行数を確認してから退避・復元する。入力検証は行ごとに最初の不備を返す関数へ変更した。 |
| SaveEmployee / SaveChange / SaveTargets | 台帳保存後の入力欄クリア・候補更新の失敗を、保存失敗と取り違えやすい。保存済みの状態を保持して案内を分けた。 |
| 台帳への文字列保存 | 履歴側のIDなどが標準書式だと、先頭ゼロの消失や意図しない型変換が起こり得る。文字列として書式を設定してから保存する。 |
| 復元失敗時 | 完全に元へ戻せた場合だけ取り消し完了と表示する。復元失敗時は再登録を避け、保守担当へ連絡する案内を表示する。 |

エラー発生後も次の文へ進む処理は、個々の操作の成否確認が必要です。修正は [Microsoft VBA On Error の仕様](https://learn.microsoft.com/en-us/office/vba/language/reference/user-interface-help/on-error-statement)に沿って見直しました。起動時に再設定するマクロ用の保護設定は維持しています。[Worksheet.Protect の仕様](https://learn.microsoft.com/en-us/office/vba/api/excel.worksheet.protect)

## この環境で確認できたこと

- ZIPのCRC、65個のXML・関連パーツの構文、内部リンクの参照先を検証した。
- マクロ有効ブックとVBAのContent Typeが正しいことを確認した。
- VBAのCFB格納構造を読み直し、25ストリームを復元できた。21モジュールの名前・ソース位置・文字コードの一致を確認した。
- 61個の手続きについて、開始・終了、同名定義、文字列の閉じ忘れ、ジャンプ先ラベルの存在を機械的に確認した。これは完全なVBA構文解析・コンパイルではない。
- 23個のボタンの接続先すべてにPublic Subが存在することを確認した。
- Workbook_Open / Workbook_SheetChange のコード、シートのコード名、テーブル名、候補の定義名の対応を確認した。
- 一括登録200行と、登録区分・氏名・店舗・役職・初期状態の入力規則の範囲を確認した。
- 新規行を先に検証し、同じ一覧の変更行を氏名から対応づけ、全行の検証後に保存する処理をコード上で点検した。
- 保存済みサンプルの2026年3～8月について、異動前後の店舗、休職中の対象外、復職後の再計上、退職翌月の対象外、個人ポイント合計と店舗目標の一致を確認した。
- コミット `3aa7c36` から `f268604` への差分は `xl/vbaProject.bin` のみで、VBA内の変更は標準モジュール `EmployeeMaster` のみ。その後、シート4のXML要素順序だけを修正した。シートの値・書式・数式・Power Query定義は変更していない。

## 実機では未確認

この環境にはWindows版Excelの実行環境がないため、VBAコンパイル、ボタンのクリック、保護状態での登録、Excel Power Queryによる再集計、Excel固有のエラー時の復元は実行していません。上記のサンプル確認は保存済みの出力に対する確認であり、Power Queryを再実行した試験ではありません。

本番運用前に保守担当がコピーで以下を確認してください。

1. Windows Excel 365で開き、修復ダイアログが出ないことを確認する。VBEの「デバッグ → VBAProjectのコンパイル」でエラーが出ないことを確認する。
2. ホームの各ボタンを開き、氏名・店舗・役職の選択ができることを確認する。
3. 新規1名と既存社員の異動1件を同じ一括登録に入れ、両方が台帳へ1件ずつ保存されることを確認する。
4. 同じ一括登録の新規社員について、翌月の異動も別行に入れて確認する。変更行を上に置いた場合も確認する。
5. ID重複、存在しない店舗、1日以外の異動日をそれぞれ入れ、確認結果に理由が出て、正常な行も含めて台帳が増えないことを確認する。
6. ID `001` の新規登録・異動を行い、従業員台帳と変更履歴のIDが両方 `001` のままになることを確認する。
7. 月別表を更新し、異動月・休職翌月・復職翌月・退職翌月と、退職月の実績加算最終日を確認する。
8. 店舗への直接入力方式で、目標の新規登録と同じ月・店舗の上書きを確認する。空欄・負数は登録できず、0は登録できることを確認する。
9. コピーを保存して閉じ、再度開いて同じ操作ができることを確認する。

ソースの閲覧用コピー：[`vba/EmployeeMaster.bas`](vba/EmployeeMaster.bas)、[`vba/ThisWorkbook.cls`](vba/ThisWorkbook.cls)。UTF-8のため、VBEへインポートする場合は環境に合った文字コードへの変換が必要です。ThisWorkbookは既存のドキュメントモジュールへ反映し、同名のクラスを追加しないでください。
